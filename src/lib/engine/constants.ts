/**
 * Engine constants — challenge passing requirements.
 *
 * In P0.4 these will be moved to EngineSettings table for runtime configuration.
 * For now: hardcoded.
 */

export const MIN_RESOLVED_POSITIONS = 35;
